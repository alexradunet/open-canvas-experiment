# Balaur Teaching Workspace

This directory contains the state and artifacts produced by the project-local `/teach` skill while learning Balaur's technologies and architecture.

The workspace may contain Markdown records, reusable assets, reference material, and executable HTML lessons. These artifacts are intentionally tracked with the repository.

Application source remains outside this directory. A teaching session must not modify application code unless the user explicitly requests that separate change.
